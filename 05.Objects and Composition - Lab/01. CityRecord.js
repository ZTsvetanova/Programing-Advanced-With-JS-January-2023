function cityRecord(name, population, treasury) {
  let record = {
    name,
    population,
    treasury,
  };
  //console.log(record);
  return record;
}
cityRecord("Tortuga", 7000, 15000);
